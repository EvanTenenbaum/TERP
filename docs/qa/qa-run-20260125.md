# QA Run Log - 2026-01-25

## Commands Executed

### pnpm typecheck

- Result: Failed (command not found; pnpm suggested `pnpm check`).

### pnpm check

- Result: Passed (tsc --noEmit).

### pnpm test

- Result: Failed.
- Failures observed:
  - `server/_core/permissionMiddleware.test.ts`: vitest mock init error (`Cannot access '__vi_import_2__' before initialization`) originating from `server/_core/simpleAuth.ts`.
  - Seed tests requiring `DATABASE_URL`:
    - `scripts/seed/seeders/index.test.ts`
    - `scripts/seed/seeders/seed-batches.test.ts`
    - `scripts/seed/seeders/seed-orders.test.ts`
    - `scripts/seed/seeders/seed-payments.test.ts`
    - `scripts/seed/seeders/seed-purchase-orders.test.ts`
    - `scripts/seed/seeders/seed-vendor-bills.test.ts`
  - `client/src/components/calendar/EventFormDialog.test.tsx`: ResizeObserver not defined (jsdom) causing 6/7 tests to fail.

### pnpm lint

- Result: Failed (command not found; pnpm suggested `pnpm eslint`).

### pnpm eslint

- Result: Failed with lint errors and warnings.
- Example errors:
  - `client/src/components/ErrorBoundary.tsx`: React not defined.
  - `client/src/components/KeyboardShortcutsModal.tsx`: array index key violations.
  - `client/src/components/UserManagement.tsx`: unused variable `errorCode`.
  - `client/src/components/VersionChecker.tsx`: unused `resetUpdateStatus`.

### pnpm build

- Result: Passed with warnings (missing VITE_APP_TITLE, chunk size warnings).
