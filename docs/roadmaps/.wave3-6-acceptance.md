### CF-009 | Field-specific validation guidance in sales pricing controls

- Errors include exact field + failed rule + fix guidance.
- Inline placement is adjacent to source control.
- Summary bar mirrors blocking errors when needed.

### CF-011 | Mode-aware primary CTA semantics (non-shipping default)

- Non-shipping mode primary CTA is Make Payment with accounting handoff.
- Shipping-enabled mode keeps explicit shipping CTA.
- CTA visibility follows RBAC and mode rules.

### CF-012 | Status controls + filtered-list reliability in core sales flow

- Status updates available in both requested locations.
- Lists do not blank unexpectedly after status change.
- Filtered-out item explanation is deterministic.

### CF-013 | Primary entry CTA rename and discoverability (`New Sale`)

- Primary entrypoint label is New Sale in target flow.
- Conflicting labels in same context are removed.
- Placement and helper affordance improve first-run discoverability.

### XP-A-001-ORD | A-001 propagation on Orders

- Filter/sort/search and quick actions follow shared placement contract on Orders.
- Sort/filter state remains stable after mutation/refresh.
- Keyboard focus order remains deterministic.

### XP-A-001-PPK | A-001 propagation on Pick & Pack

- Filter/sort/search and quick actions follow shared placement contract on Pick & Pack.
- Sort/filter state remains stable after mutation/refresh.
- Keyboard focus order remains deterministic.

### XP-A-002-ORD | A-002 propagation on Orders

- Right-drawer quick-edit on Orders uses unified open/save/close contract.
- Deep-link targets open to expected section.
- Unsaved context is preserved across drawer interactions.

### XP-A-003-ORD | A-003 propagation on Orders

- Primary action semantics on Orders are mode-aware and explicit.
- Non-shipping defaults avoid shipping-first CTA in this business unit.
- Role-based visibility rules are enforced.

### XP-A-003-PPK | A-003 propagation on Pick & Pack

- Primary action semantics on Pick & Pack are mode-aware and explicit.
- Non-shipping defaults avoid shipping-first CTA in this business unit.
- Role-based visibility rules are enforced.

### XP-A-004-ORD | A-004 propagation on Orders

- Status transitions on Orders match approved transition map.
- Post-mutation list never blanks unexpectedly.
- Filtered-out movement message is explicit.

### XP-A-004-PPK | A-004 propagation on Pick & Pack

- Status transitions on Pick & Pack match approved transition map.
- Post-mutation list never blanks unexpectedly.
- Filtered-out movement message is explicit.

### XP-A-005-ORD | A-005 propagation on Orders

- Validation messages on Orders include field + rule + fix guidance.
- Blocking errors are adjacent to source input.
- Summary-level messaging exists for cross-field failures.

### XP-A-005-PPK | A-005 propagation on Pick & Pack

- Validation messages on Pick & Pack include field + rule + fix guidance.
- Blocking errors are adjacent to source input.
- Summary-level messaging exists for cross-field failures.

### XP-A-006-ORD | A-006 propagation on Orders

- Quantity controls on Orders enforce integer step=1.
- Paste/typing edge cases normalize deterministically.
- Downstream recalculation remains accurate.

### XP-A-006-PPK | A-006 propagation on Pick & Pack

- Quantity controls on Pick & Pack enforce integer step=1.
- Paste/typing edge cases normalize deterministically.
- Downstream recalculation remains accurate.

### XP-A-007-ORD | A-007 propagation on Orders

- Post-save behavior on Orders follows return_to_origin by default.
- Failed saves preserve edits/context.
- Close/reopen behavior is deterministic.

### XP-A-007-PPK | A-007 propagation on Pick & Pack

- Post-save behavior on Pick & Pack follows return_to_origin by default.
- Failed saves preserve edits/context.
- Close/reopen behavior is deterministic.

### XP-A-008-ORD | A-008 propagation on Orders

- Orders defaults to compact density tokens on high-throughput views.
- Readability and control discoverability stay within UX guardrails.
- No required action falls below viewport accessibility threshold.

### XP-A-008-PPK | A-008 propagation on Pick & Pack

- Pick & Pack defaults to compact density tokens on high-throughput views.
- Readability and control discoverability stay within UX guardrails.
- No required action falls below viewport accessibility threshold.

### XP-A-001-ACC | A-001 propagation on Accounting

- Filter/sort/search and quick actions follow shared placement contract on Accounting.
- Sort/filter state remains stable after mutation/refresh.
- Keyboard focus order remains deterministic.

### XP-A-001-CLI | A-001 propagation on Clients

- Filter/sort/search and quick actions follow shared placement contract on Clients.
- Sort/filter state remains stable after mutation/refresh.
- Keyboard focus order remains deterministic.

### XP-A-002-ACC | A-002 propagation on Accounting

- Right-drawer quick-edit on Accounting uses unified open/save/close contract.
- Deep-link targets open to expected section.
- Unsaved context is preserved across drawer interactions.

### XP-A-002-CLI | A-002 propagation on Clients

- Right-drawer quick-edit on Clients uses unified open/save/close contract.
- Deep-link targets open to expected section.
- Unsaved context is preserved across drawer interactions.

### XP-A-005-ACC | A-005 propagation on Accounting

- Validation messages on Accounting include field + rule + fix guidance.
- Blocking errors are adjacent to source input.
- Summary-level messaging exists for cross-field failures.

### XP-A-005-CLI | A-005 propagation on Clients

- Validation messages on Clients include field + rule + fix guidance.
- Blocking errors are adjacent to source input.
- Summary-level messaging exists for cross-field failures.

### XP-A-007-ACC | A-007 propagation on Accounting

- Post-save behavior on Accounting follows return_to_origin by default.
- Failed saves preserve edits/context.
- Close/reopen behavior is deterministic.

### XP-A-007-CLI | A-007 propagation on Clients

- Post-save behavior on Clients follows return_to_origin by default.
- Failed saves preserve edits/context.
- Close/reopen behavior is deterministic.

### XP-A-008-ACC | A-008 propagation on Accounting

- Accounting defaults to compact density tokens on high-throughput views.
- Readability and control discoverability stay within UX guardrails.
- No required action falls below viewport accessibility threshold.

### XP-A-008-CLI | A-008 propagation on Clients

- Clients defaults to compact density tokens on high-throughput views.
- Readability and control discoverability stay within UX guardrails.
- No required action falls below viewport accessibility threshold.

### XP-A-001-INV | A-001 propagation on Inventory

- Filter/sort/search and quick actions follow shared placement contract on Inventory.
- Sort/filter state remains stable after mutation/refresh.
- Keyboard focus order remains deterministic.

### XP-A-004-INV | A-004 propagation on Inventory

- Status transitions on Inventory match approved transition map.
- Post-mutation list never blanks unexpectedly.
- Filtered-out movement message is explicit.

### XP-A-005-INV | A-005 propagation on Inventory

- Validation messages on Inventory include field + rule + fix guidance.
- Blocking errors are adjacent to source input.
- Summary-level messaging exists for cross-field failures.

### XP-A-006-INV | A-006 propagation on Inventory

- Quantity controls on Inventory enforce integer step=1.
- Paste/typing edge cases normalize deterministically.
- Downstream recalculation remains accurate.

### XP-A-007-INV | A-007 propagation on Inventory

- Post-save behavior on Inventory follows return_to_origin by default.
- Failed saves preserve edits/context.
- Close/reopen behavior is deterministic.

### XP-A-008-INV | A-008 propagation on Inventory

- Inventory defaults to compact density tokens on high-throughput views.
- Readability and control discoverability stay within UX guardrails.
- No required action falls below viewport accessibility threshold.
