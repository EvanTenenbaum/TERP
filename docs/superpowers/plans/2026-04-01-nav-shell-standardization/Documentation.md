# Documentation Notes

## User-Facing Outcome

TERP now uses a compact utility bar and a shared workspace strip so primary work surfaces get more usable vertical space before the first actionable row.

## Validation Targets

- `/sales?tab=sales-sheets&surface=sheet-native`
- `/sales?tab=orders`
- `/inventory`
- `/accounting`

## Rollout Notes

- Shared shell changes should be treated as the new default pattern for future workspace pages
- Surface-level toolbars should avoid duplicate navigation/context rows and keep local actions close to the working canvas
