# Discrepancy Log: Operations -> Direct Intake

Snapshot:

- Commit: `0e2bb8ba5edfb9c9b1e1b836acb50a8c9348cfae`
- Extracted: `2026-03-19`
- Checked Against Code: `targeted current-code procedure confirmation`

| ID         | Criticality | Discrepancy                                                                                                                                                            | Evidence                                                                                                                                                      | Impact on Ledger                                                        | Required Resolution Before Blueprint Approval                                                    | Status   |
| ---------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------- |
| `INT-D001` | `P1`        | `FEATURE_PRESERVATION_MATRIX.md` still labels `DF-077` as a different feature, while `USER_FLOWS.md` uses `DF-077` for the Direct Intake Work Surface.                 | [USER_FLOWS.md](../../../docs/features/USER_FLOWS.md), [FEATURE_PRESERVATION_MATRIX.md](../../../docs/specs/ui-ux-strategy/FEATURE_PRESERVATION_MATRIX.md)    | intake ledger traceability is correct here, but cross-doc drift remains | reconcile preservation-matrix labeling so future agents do not inherit the wrong feature binding | open     |
| `INT-D002` | `P1`        | Receipt and discrepancy adjacency are real operational dependencies, but the current intake artboards and pack-level docs still under-model them.                      | [USER_FLOWS.md](../../../docs/features/USER_FLOWS.md), [DirectIntakeWorkSurface.tsx](../../../client/src/components/work-surface/DirectIntakeWorkSurface.tsx) | intake cannot be blueprint-approved as a fully self-contained surface   | define which receipt and discrepancy surfaces stay adjacent during the intake blueprint pass     | open     |
| `INT-D003` | `P1`        | The review recording pushed toward less tutorialized framing, but that can be misread as permission to simplify away validation, branch distinction, or media cleanup. | recording-derived packet in `artifacts/video-feedback/2026-03-19-figma-review/` plus current intake code                                                      | the ledger had to make those non-negotiable behaviors explicit          | keep layout simplification separate from functionality reduction during blueprinting             | resolved |
| `INT-D004` | `P2`        | Terminology still drifts between `Receiving` and `Intake` across routes, docs, and visible labels.                                                                     | terminology policy plus current route names in code and docs                                                                                                  | visible copy can drift even when behavior is preserved                  | apply terminology reconciliation during blueprint copy work without breaking route compatibility | open     |
