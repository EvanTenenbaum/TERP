# BUG-006: Debug Dashboard Visible in Production

<!-- METADATA (for validation) -->
<!-- TASK_ID: BUG-006 -->
<!-- TASK_TITLE: Hide Debug Dashboard in Production -->
<!-- PROMPT_VERSION: 1.0 -->
<!-- LAST_VALIDATED: 2025-11-22 -->

**Repository:** https://github.com/EvanTenenbaum/TERP  
**Task ID:** BUG-006  
**Priority:** P3 (LOW - COSMETIC)  
**Estimated Time:** 30 minutes  
**Module:** Orders

---

## 🎯 Context

**Problem:**
A red debug box (query info, loading state) is visible on the Orders page in production.

**Goal:**
Hide this debug information in production environments.

---

## Phase 1: Fix

### Step 1.1: Locate Component
1. Check `client/src/pages/OrdersPage.tsx`.
2. Look for a component rendering query stats (e.g., `<pre>`, red border div).

### Step 1.2: Add Environment Guard
1. Wrap the debug component in a conditional check.
   ```tsx
   {process.env.NODE_ENV === 'development' && (
     <div className="debug-panel">...</div>
   )}
   ```
   OR remove it entirely if it's no longer needed.

### Step 1.3: Verify
1. Run locally (dev mode) -> Should be visible (or hidden if you chose to remove).
2. Build/Preview mode -> Should be hidden.

---

## Phase 2: Completion

### Step 2.1: Commit
```bash
git add .
git commit -m "Fix BUG-006: Hide debug dashboard in production"
git push origin bug-006-fix:main
```

### Step 2.2: Update Roadmap
- Mark `BUG-006` as `✅ COMPLETE`.
